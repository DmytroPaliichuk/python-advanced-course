"""
Нижче наведено реалістичні ситуації з продакшен-проєктів, у яких потрібно визначити:
1) Чи впливає GIL на дану задачу?
2) Яку модель багатозадачності слід обрати: багатопоточність чи багатопроцесність?
Пояснити коротко - чому саме такий вибір.

Мета завдання - навчитися правильно читати вимоги, визначати тип навантаження (CPU-bound чи I/O-bound)
та робити обґрунтований вибір між потоками та процесами.
"""

"""
1. Масовий перерахунок метрик у Python-циклах

Команда ETL рахує щогодини агрегації по великому дампу логів (десятки мільйонів записів).
Дані вже в оперативці як список Python-словників.
Потрібно пройтись по кожному запису, виконати кілька умовних перевірок, розрахувати метрики й записати
результат назад в Postgres. Весь розрахунок написаний на чистому Python без NumPy/C-розширень.
Є вимога зменшити час розрахунку з 40 хвилин до 10–15 хвилин за рахунок розпаралелювання на одній машині.
"""

"""
2. Масове скачування JSON з зовнішнього API

Сервіс має зібрати дані з зовнішнього REST API: ~10 000 запитів за одну синхронізацію.
Розмір відповіді - невеликий JSON, над яким виконується мінімальна обробка
(перейменування полів, кілька простих перевірок) і запис у Kafka.
Основний час витрачається на очікування відповіді від API.
Необхідно максимально скоротити загальний час синхронізації.
"""

"""
3. Обчислення SHA-256 для великої кількості файлів

Потрібно перевірити цілісність 50 000 файлів по 50–200 MB.
Для кожного файлу треба порахувати SHA-256 і записати результат у базу.
Основний час - це читання файлів і обчислення хешу.
Використовується стандартний hashlib у Python.
"""

"""
4. Конвертація медіа через зовнішній інструмент (ffmpeg)

Бекенд сервіс приймає відео від користувачів і має для кожного файлу:
	- згенерувати прев’ю (короткий кліп),
	- витягти кілька скріншотів.

Для цього запускається ffmpeg через subprocess.run.
Python-частина - це: підготувати команду, запустити процес, дочекатися завершення,
оновити статус у БД. Один сервер може обробляти десятки таких задач паралельно.
"""

"""
5. Попередня обробка зображень для ML пайплайну

Перед тренуванням моделі потрібно:
	- прочитати 200 тис. зображень з диску,
	- змінити розмір,
	- нормалізувати пікселі,
	- зберегти тензори в готовий формат (наприклад, .npy/HDF5).

Обробка робиться через бібліотеку, яка частково реалізована на C і вміє відпускати GIL
під час важких операцій над масивами (наприклад, Pillow + NumPy).
"""

"""
6. Валідація великих JSON-повідомлень у черзі

У вас є сервіс, який читає повідомлення з Kafka.
Кожне повідомлення - великий JSON (складні вкладені структури).
Необхідно:
	- розпарсити JSON,
	- прогнати через складну валідаційну логіку на чистому Python (багато if-ів, перетворень, нормалізацій),
	- у разі успіху - записати в базу, у разі помилки - надіслати в dead-letter чергу.

Стало помітно, що CPU завжди на 100%, а споживання Kafka-черги відстає.
"""

"""
7. Побудова рекомендацій офлайн

Офлайн-сервіс раз на годину будує матрицю "користувач -> товар" і рахує схожість (англ. cosine similarity) між векторами.
Логіка: читання даних з бази, побудова великих NumPy-матриць, далі - інтенсивні векторизовані операції (матриця * матриця).
Весь розрахунок займає 45 хвилин, потрібно зменшити до ~10 хвилин.
Основний час, судячи з профілювання, - на матричних операціях.
"""

"""
8. Генерація великих CSV-звітів на льоту

Адмін-панель дозволяє згенерувати CSV-звіт на 1–2 млн рядків.
Запит до бази відбувається потоково (cursor/streaming), далі кожен рядок форматується
у CSV і пишеться у відповідь HTTP (streaming).
Потрібно робити до 5 таких запитів паралельно на одному бекенд-сервері.
"""
