"""
1. Масовий перерахунок метрик у Python-циклах

GIL:
    - Так, заважає: обчислення CPU-bound, логіка в чистому Python, один потік реально рахує в кожен момент часу.

Стратегія:
    - Багатопроцесність.

Чому:
    - Кожен процес має власний GIL.
    - CPU-навантаження добре масштабується по ядрах.
    - Потоки не дадуть суттєвого виграшу, бо блокуються GIL під час обчислень.
"""

"""
2. Масове скачування JSON з зовнішнього API

GIL:
    - Практично не заважає: більшість часу потік чекає мережевої відповіді, під час I/O GIL відпускається.

Стратегія:
    - Багатопотоковість (або async, але з двох - потоки).

Чому:
    - Це I/O-bound задача (очікування HTTP).
    - Потоки дозволяють одночасно тримати багато запитів "у польоті".
    - Старт процесів тут зайвий оверхед, CPU-навантаження мінімальне.
"""

"""
3. Обчислення SHA-256 для великої кількості файлів

GIL:
    - Так, частково заважає: обчислення хешу - CPU-bound, і значна частина роботи виконується в Python-процесі з одним GIL.

Стратегія:
    - Багатопроцесність.

Чому:
    - Велике CPU-навантаження добре масштабується по ядрах.
    - Процеси дозволяють паралельно рахувати хеші без блокування GIL.
    - Потоки дадуть виграш лише на I/O (читання), але не на CPU-частині.
"""

"""
4. Конвертація медіа через зовнішній інструмент (ffmpeg)

GIL:
    - Не заважає суттєво: важка робота виконується в окремому процесі ffmpeg, Python в основному чекає завершення.

Стратегія:
    - Багатопотоковість.

Чому:
    - Кожен потік просто керує окремим subprocess + I/O.
    - Потоки добре підходять для orchestration-завдань, де важка робота – у зовнішніх процесах.
    - Піднімати окремі Python-процеси поверх ffmpeg-процесів - зайвий оверхед.
"""

"""
5. Попередня обробка зображень для ML пайплайну

GIL:
    - Частково неактуальний: важкі операції над масивами виконуються в C-коді з відпусканням GIL.

Стратегія:
    - Багатопоточність (як перший вибір).

Чому:
    - Бібліотека виконує важкі операції поза GIL.
    - Потоки дозволяють паралельно завантажувати та обробляти кілька зображень.
    - Процеси можна розглядати, лише якщо обсяги даних настільки великі, що overhead між потоками й процесами вже неважливий.
"""

"""
6. Валідація великих JSON-повідомлень у черзі

GIL:
    - Так, заважає: логіка валідації і перетворень - чистий Python, CPU-bound, один потік "тримає" GIL.

Стратегія:
    - Багатопроцесність.

Чому:
    - Основне вузьке місце - CPU, а не Kafka I/O.
    - Кілька процесів-воркерів можуть паралельно валідовувати різні повідомлення.
    - Потоки мало допоможуть при повністю CPU-bound валідаційній логіці.
"""

"""
7. Побудова рекомендацій офлайн

GIL:
    - Зазвичай мало заважає: NumPy часто відпускає GIL під час важких операцій, що виконуються в C.

Стратегія:
    - Спочатку - оптимізувати в рамках одного процесу (векторизація тощо).
    - Якщо потрібна паралельність: або багатопроцесність (розбити завдання на блоки), або спеціалізовані фреймворки (Dask/Ray).

Якщо вибирати строго між потоками і процесами:
    - Більш безпечний вибір - багатопроцесність, оскільки розподіл обчислень по блоках ізольований і не залежить від деталей реалізації щодо GIL.
"""

"""
8. Генерація великих CSV-звітів на льоту

GIL:
    - Майже не заважає: основне навантаження - I/O (база + мережа) та прості операції форматування.

Стратегія:
    - Багатопотоковість.

Чому:
    - Кожен потік може обслуговувати окремий запит і стрімити дані клієнту.
    - Форматування рядків не настільки важке, щоб робити окремі процеси.
    - GIL не є основним вузьким місцем, вузьке місце - швидкість БД/мережі.
"""
